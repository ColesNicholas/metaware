# Manual calculation of M's and SD's from data reported in Table 2 
# of 'Effects of experimental context, demand characteristics, and 
# situational cues: New data'

# experimental subject data
exp <- c(2, 0, 0, 0, 1, 
         0, 0, 0, 0, 0)

mean(exp)
sd(exp)

# control group data
cont <- c(4, 0, 0, 0, 0, 
          0, 3, 0, 0, 0)

mean(cont)
sd(cont)